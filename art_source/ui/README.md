# UI art source

Canonical UI source sheets used by the offline slicer live in `extracted/`.
Godot ignores this directory; generated or approved runtime outputs belong in
`assets/ui/runtime/`. Do not reference these files from production scenes.
