# Art source

Non-runtime source artwork and generated candidates live here. Files under this directory are excluded from Godot import by `.gdignore`; promote an approved asset into `assets/` and register it in the relevant manifest before using it at runtime.
