# Temporary compatibility sources

Only the two small battle button sheets remain here because legacy unreferenced
root StyleBoxes still point to them. Batch F removes those styles and completes
the move to `art_source/ui/extracted/`. Production scenes do not reference this
directory.
